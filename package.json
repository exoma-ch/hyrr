{
  "//name": "Keep this. Without it npm derives the workspace-root name from the DIRECTORY and writes that into package-lock.json, so every `npm install` in a git worktree dirties the tree (#662).",
  "name": "hyrr",
  "private": true,
  "workspaces": [
    "packages/*",
    "frontend"
  ],
  "scripts": {
    "test:packages": "vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "playwright": "^1.58.2"
  }
}
